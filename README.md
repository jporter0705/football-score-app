# football-score-app
Personal football scoreboard and betting tracker

Run `node server.cjs` for local preview, `npm test` for checks, and `npm run build`
for the public assets. See [PREVIEW.md](PREVIEW.md) and
[PRIVATE-STORAGE.md](PRIVATE-STORAGE.md) for persistence and migration details.
